import { DefaultEditor } from "./DefaultEditor";
export class AutoCompleteBox extends DefaultEditor {
  constructor(context, storeId) {
    super({ context, value: "AutoCompleteBox", storeId });
  }
}
